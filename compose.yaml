services:
  db:
    image: pgvector/pgvector:pg18-trixie
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-pawdex}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pawdex_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-pawdex}
      PGDATA: /var/lib/postgresql/data
    volumes:
      - pawdex-db-data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    # Not published to the host: reachable only on the internal compose network.
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-pawdex} -d ${POSTGRES_DB:-pawdex}"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 512m

  minio:
    image: minio/minio
    restart: unless-stopped
    command: server /data
    environment:
      MINIO_ROOT_USER: ${PAWDEX_S3_ACCESS_KEY:-pawdex}
      MINIO_ROOT_PASSWORD: ${PAWDEX_S3_SECRET_KEY:-pawdex_minio_secret}
    volumes:
      - pawdex-minio-data:/data
    # Internal only; crops are served through the Next.js /api/media proxy.
    expose:
      - "9000"
    deploy:
      resources:
        limits:
          memory: 512m

  ml-api:
    build:
      context: ./ml-api
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-pawdex}:${POSTGRES_PASSWORD:-pawdex_dev_password}@db:5432/${POSTGRES_DB:-pawdex}
      PAWDEX_YOLO_MODEL: ${PAWDEX_YOLO_MODEL:-yolo11n.pt}
      PAWDEX_YOLO_CONFIDENCE: ${PAWDEX_YOLO_CONFIDENCE:-0.35}
      PAWDEX_ALLOWED_ORIGINS: ${PAWDEX_ALLOWED_ORIGINS:-http://localhost:3000,http://127.0.0.1:3000}
      PAWDEX_INTERNAL_TOKEN: ${PAWDEX_INTERNAL_TOKEN:-pawdex-dev-internal-token}
      PAWDEX_S3_ENDPOINT: ${PAWDEX_S3_ENDPOINT:-minio:9000}
      PAWDEX_S3_ACCESS_KEY: ${PAWDEX_S3_ACCESS_KEY:-pawdex}
      PAWDEX_S3_SECRET_KEY: ${PAWDEX_S3_SECRET_KEY:-pawdex_minio_secret}
      PAWDEX_S3_BUCKET: ${PAWDEX_S3_BUCKET:-pawdex}
      PAWDEX_S3_SECURE: ${PAWDEX_S3_SECURE:-false}
    # Reachable only via the Next.js proxy on the internal network, never the host.
    expose:
      - "8000"
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 4g

  web:
    build:
      context: .
    restart: unless-stopped
    environment:
      ML_API_URL: http://ml-api:8000
      NODE_ENV: production
      AUTH_SECRET: ${AUTH_SECRET:-pawdex-dev-auth-secret-change-me}
      PAWDEX_INTERNAL_TOKEN: ${PAWDEX_INTERNAL_TOKEN:-pawdex-dev-internal-token}
      PAWDEX_ENABLE_DEV_AUTH: ${PAWDEX_ENABLE_DEV_AUTH:-true}
    ports:
      - "3000:3000"
    depends_on:
      ml-api:
        condition: service_healthy
    healthcheck:
      test:
        - "CMD-SHELL"
        - "node -e \"fetch('http://localhost:3000/').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pawdex-db-data:
  pawdex-minio-data:
